# v4.1.0 build progress

One section per plan box, added when the box is ticked. Each section: what changed,
commit ID(s), the exact proof command(s) and their pass line. No proof, no tick.

## A1: baseline on David's computer (Windows 11, Python 3.12.10, uv 0.12.17)

Branch `release/v4.1.0` created from `handoff/v4.1.0-base` (5eeada3). `uv sync --dev` clean.
The handoff baseline was measured on Linux; the suite did not collect on Windows, so the
environment was fixed first (all test-only or portability-only, no behaviour change):

- `extensions/feature_workflow/{coordinator,run_state}.py` imported `fcntl` (POSIX only) and
  used `os.O_DIRECTORY` for the directory fsync. Added `extensions/feature_workflow/_filelock.py`
  (fcntl on POSIX, msvcrt on Windows) with `tests/test_feature_workflow_filelock.py` (2 tests);
  the directory fsync is skipped where the flag does not exist.
- `tests/conftest.py`: Windows-only patch for a CPython quirk — cancelling a task while it is
  inside `create_subprocess_exec` leaves the transport's `_wait()` unresolved forever, which
  hung pytest-asyncio's loop teardown (first seen in `test_task_loop_cog.py`).
- `tests/test_codex_runner.py`: the interruptible fake process now honours `terminate()`,
  which is what `CodexRunner.interrupt()` calls on Windows (the test hung otherwise).
- Path-separator-only assertion fixes: `test_feature_workflow.py` (manifest paths are POSIX by
  contract → `as_posix()`), `test_project_catalog.py` (real tmp roots use native `PurePath`;
  the two symlink tests skip when the OS refuses symlinks), `test_collision_detection.py`,
  `test_transcript_lookup.py`; `render_preview.preview_name()` now returns a POSIX name on
  every OS (its docstring contract).
- Linux ops-script tests (`test_cleanup_worktrees_script.py`, `test_deploy_drift_script.py`,
  `test_health_check_script.py`, `test_pre_start_script.py`) are `skipif(win32)`: they check
  executable bits and put bash-shebang stubs on PATH.
- Machine setup: installed `uv`, set a global git identity (tests commit inside fresh clones).

Proof (2026-09-21):

```
uv run ruff check claude_discord/ claude_code_core/ claude_teams/      → All checks passed!
uv run ruff format --check claude_discord/ claude_teams/ claude_code_core/ → 194 files already formatted
uv run pyright claude_discord/ claude_teams/ claude_code_core/          → 0 errors, 6 warnings
scripts/test-clean-env.sh → 4523 passed, 35 skipped, 10 warnings in 121.52s
```

4523 + 35 = 4558 = the 4556 Linux baseline + 2 new lock tests. Of the 35 skips, 29 are
Windows-only (27 ops-script tests + 2 symlink tests); the other 6 skipped on Linux too.

## B1: merge `origin/save/local-main-2026-09-21`

68 commits merged (`git merge --no-ff`). Conflicts and how they were resolved:

- `claude_discord/cogs/claude_chat.py`: only a `# Ignore bot messages` comment differed;
  both sides' behaviour is identical in the result (bot messages still go through
  `_try_receive_handoff_message`).
- `.planning/davids-computer/three-bot-flow.md`: kept HEAD's (53 lines, 2026-09-19) over
  the branch's (41 lines, 2026-09-18) — the newer, fuller text.
- `tests/test_collision_detection.py`: kept the A1 separator-neutral assertion.
- Dropped the broken absolute symlink `examples/ebibot/cogs/workdir_command.py`
  (see decisions file, B1).

What the merge actually adds: `examples/project_creation/` (standalone new-project /
GitHub clone menu), `extensions/project_picker/` (fork-derived `/cd` `/cdnew` picker +
its tests), docs/planning, 4 lines in `scripts/health-check.sh`. No `claude_discord/`
or `claude_code_core/` source changed: the launcher/session-control commits on that
branch (`3425cd6`, `deadd2b`) already landed in the base under other IDs.

Proof:

```
git grep -n "gh.*clone\|repo.*clone" examples/project_creation/project_creation.py
  → project_creation.py:140: await run_gh("repo", "clone", url, str(staging))
git grep -n 'label="Favorite folders"' claude_discord/cogs/project_launcher.py
  → project_launcher.py:60: @discord.ui.button(label="Favorite folders", custom_id="ccdb:launcher:favorites:v1")
git grep -n "_shortcut_task" claude_discord/cogs/project_launcher.py
  → project_launcher.py:387 (the deadd2b "keep session controls visible" fix)
uv run ruff check claude_discord/ claude_code_core/ claude_teams/ → All checks passed!
uv run ruff format --check claude_discord/ claude_teams/ claude_code_core/ → 194 files already formatted
scripts/test-clean-env.sh → 4523 passed, 35 skipped, 13 warnings in 121.22s
```

Note for B4: `extensions/project_picker/tests` (not in CI's `testpaths`) has 4 tests that
fail on Windows on path separators only (`uv run pytest extensions/project_picker/tests
examples/project_creation/tests` → 4 failed, 62 passed).

## B2: automatic voice transcripts (`extensions/voice_transcripts/`)

Cherry-picked `1baab0b`, `e581115`, `c5ac2cf` from `origin/deploy/drew-ai-voice-transcripts`
as `87f43ea`, `622d943`, `c5f3aef`. All three touch only `extensions/voice_transcripts/`,
which the base lacked; the Python `voice_recorder` cog is untouched (different feature:
per-recording files vs. a live Node companion on the same bot identity). No conflicts.

Proof (the extension ships its own Node test suite; Node 22.20.0 portable install):

```
cd extensions/voice_transcripts && npm ci --omit=dev && npm test
  → # tests 25 / # pass 25 / # fail 0
scripts/test-clean-env.sh → unchanged (no Python files touched)
```

## B3: structured worker test evidence + harness research doc

- The base's `extensions/feature_workflow/coordinator.py` still required every `tests[]`
  entry to be a string (`isinstance(test, str) and test.strip()`), so `e5c3293` was genuinely
  missing. Cherry-picked as `f823a32`: adds `_is_test_evidence()` (string, or an object with
  non-empty `command` and `outcome`) and 2 tests in `tests/test_feature_workflow.py`.
- `docs/open-source-coding-agent-harness-alternatives.md` from
  `origin/lockin/feature-workflow-runtime` copied unchanged to
  `docs/research/open-source-coding-agent-harness-alternatives.md` (125 lines, research
  dated 2026-09-07).

Proof:

```
scripts/test-clean-env.sh tests/test_feature_workflow.py → 42 passed (was 40)
uv run ruff check extensions/feature_workflow/ → All checks passed!
```

## B4: one folder picker

Deleted `extensions/project_picker/` (see decisions, B4). Kept the framework launcher
(`claude_discord/cogs/project_launcher.py`) and `examples/project_creation/` (`/new`).
`LOCKIN-AI.md` now points at the launcher instead of the deleted extension.

Proof:

```
git grep -l "extensions/project_picker" -- . ':!docs/' ':!.planning/'  → (nothing)
scripts/test-clean-env.sh tests/test_example_cogs_load.py tests/test_project_launcher.py tests/test_project_catalog.py
  → 149 passed, 2 skipped
```

## B5: every remaining inventory branch, commit by commit

Method: for each unique commit in `docs/plans/v4.1.0-branch-inventory.md` (excluding the
`main` section, which B1 merged), every added non-trivial line of its patch was searched
verbatim in the release tree's version of the same file; anything under 95 % was read by
hand. "present (evolved)" = the feature landed and was changed again later.

| commit | subject | on branches | status | evidence |
|---|---|---|---|---|
| `1d9d71d` | chore: save untracked work before handoff | feat/task-loop | dropped | `.gowork-catchup.md` status note from the Go Work v3 era; superseded by `docs/plans/gowork-upgrade-execution-plan.md` + its progress file |
| `b9838ba` | feat: plan-card previews for *.plan.json | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present (evolved) | plan cards live in `claude_discord/discord_ui/plan_card.py` (Markdown renderer with `_clip`), later than this HTML version |
| `1cb3dc9` | revert: drop unrelated markdown reformatting swept in by an  | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present | revert of a reformat; the reverted state is what HEAD has |
| `46790c6` | fix(dsh): read the route-file override when it is used, and  | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present | 31/32 added lines found verbatim in the release tree |
| `1052886` | test: pre-start must name every extra the runtime needs | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present | 1/1 added lines found verbatim in the release tree |
| `e9d5023` | fix(dsh): make Stop actually stop, and stop dropping images  | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present (evolved) | 113/134 lines verbatim; the rest is a reworded docstring in `dsh_backend.py` |
| `7ce7473` | chore: install the deepseek extra in pre-start | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present | 1/1 added lines found verbatim in the release tree |
| `054b070` | chore: install the deepseek extra in pre-start | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present | 1/1 added lines found verbatim in the release tree |
| `af4b592` | feat: add the dsh backend — DeepSeek Harness, hosting more t | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present (evolved) | 1133/1194 lines verbatim; landed via `8402ae7` consolidate-main then hardened by `46790c6`/`468792f` |
| `2ce47ac` | fix(voice_recorder): swallow OpusError from corrupt packets, | chore/consolidate-main, feat/voice-recorder-cog, fix/dsh-review-findings, save/project-picker-extension | present (superseded) | `b65b55a fix(voice_recorder): use opus packet-loss concealment` replaced swallowing OpusError |
| `24567c4` | feat(ebibot): voice channel recorder Cog | chore/consolidate-main, fix/dsh-review-findings, save/project-picker-extension | present (evolved) | 166/182 lines verbatim; `examples/ebibot/cogs/voice_recorder.py` has 5 later fixes (#13–#17) |
| `aa4ae5e` | chore: automate distribution and runtime recovery | chore/production-hardening | present | 167/167 added lines found verbatim in the release tree |
| `5b1a5be` | fix: clear product certification blockers | chore/production-hardening | present | 6/6 added lines found verbatim in the release tree |
| `5fa89a6` | fix: explain preserved worktrees safely | chore/production-hardening, fix/discord-project-routing, lockin/feature-workflow-runtime | present | 25/25 added lines found verbatim in the release tree |
| `03920fb` | fix: resume project-local worktrees from containers | chore/production-hardening, fix/discord-project-routing, lockin/feature-workflow-runtime | present | 17/17 added lines found verbatim in the release tree |
| `c690c6c` | fix: keep session worktrees inside projects | chore/production-hardening, fix/discord-project-routing, lockin/feature-workflow-runtime | present | 133/138 added lines found verbatim in the release tree |
| `811aa42` | fix: keep saved project binding authoritative | chore/production-hardening, fix/discord-project-routing, lockin/feature-workflow-runtime | present | 40/40 added lines found verbatim in the release tree |
| `520c555` | fix: preserve project directory across Discord sessions | chore/production-hardening, fix/discord-project-routing, lockin/feature-workflow-runtime | present (evolved) | `bind_working_dir` became `SessionRepository.ensure_working_dir` (`811aa42`), called from `_run_helper.py:421`, which also re-applies the saved binding |
| `6690a01` | docs: target retained workflow runtime for deployment | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 1/1 added lines found verbatim in the release tree |
| `30bb23b` | docs: record published Discord workflow instructions | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 7/7 added lines found verbatim in the release tree |
| `42a68fc` | docs: activate verified Lockin feature workflow and everyday | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 289/289 added lines found verbatim in the release tree |
| `13de390` | test: verify integrated results from both Discord feature bu | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 40/40 added lines found verbatim in the release tree |
| `49386f9` | fix: distinguish queued turns and admit feature workers by d | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 321/322 added lines found verbatim in the release tree |
| `8cc12db` | feat: add trial whitespace word counter | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 24/24 added lines found verbatim in the release tree |
| `ad09478` | feat: add approved trial greeting with focused unittest cove | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 24/24 added lines found verbatim in the release tree |
| `986d1ec` | fix: admit approved builders to the existing Ebi session que | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present (evolved) | `Coordinator(queue_ready=True)` is the default now (`49386f9`); the `--queue-ready` flag text changed with it |
| `998eac6` | docs: normalize verification log whitespace | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | whitespace-only doc normalisation; nothing to port |
| `8bd8b58` | docs: preserve trial evidence and automatic integration hand | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 1291/1438 lines verbatim; the rest are trial-evidence timestamps/logs of the same run |
| `7782f80` | feat: coordinate approved OpenSpec features through isolated | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 1027/1042 added lines found verbatim in the release tree |
| `e663639` | docs: plan isolated trial-word-count feature | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 65/65 added lines found verbatim in the release tree |
| `462c09c` | docs: plan isolated trial-greeting feature | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 73/73 added lines found verbatim in the release tree |
| `1b19dba` | docs: establish isolated OpenSpec workflow trial foundation | chore/production-hardening, fix/lockin-concurrency-review, lockin/feature-workflow-runtime, plan/codex-native-harness | present | 804/804 added lines found verbatim in the release tree |
| `742b9e0` | fix: make owner auto merge opt in | chore/standalone-repository | present | code lines in `.github/workflows/auto-approve.yml` + test; the doc lines are in `.agents/skills/release/SKILL.md` (the `.claude` path is a symlink) |
| `968b298` | chore: detach standalone repository operations | chore/standalone-repository | present | all code lines verbatim; 7 missing lines are README prose |
| `c5ac2cf` | fix(voice_transcripts): log DAVE decrypt failures and per-sp | deploy/drew-ai-voice-transcripts, session/1547448027776811038-voice | ported (B2) | cherry-picked as `c5f3aef` |
| `65ab7b9` | docs: record successful live voice transcription test | deploy/drew-ai-voice-transcripts, session/1547448027776811038-voice | ported (B5) | cherry-picked as `e3ce5c4` (voice transcripts VERIFICATION.md) |
| `e581115` | fix: keep voice deployment and recordings outside session cl | deploy/drew-ai-voice-transcripts, session/1547448027776811038-voice | ported (B2) | cherry-picked as `622d943` |
| `1baab0b` | feat: add automatic voice transcripts using existing bot ide | deploy/drew-ai-voice-transcripts, session/1547448027776811038-voice | ported (B2) | cherry-picked as `87f43ea` |
| `5d28da9` | docs: handoff for the next agent — dsh review findings, fixe | fix/dsh-review-findings, save/project-picker-extension | present | 133/133 added lines found verbatim in the release tree |
| `22b8be7` | fix: plan-card previews reach Discord; per-turn system conte | fix/dsh-review-findings, save/project-picker-extension | present (evolved) | same: `plan_card.py` has `_clip`, non-dict step tolerance and the JSON-not-object log; per-turn system context is in `_run_helper.py` |
| `468792f` | fix(dsh): session lineage across model switches, runtime hom | fix/dsh-review-findings, save/project-picker-extension | present | 156/156 added lines found verbatim in the release tree |
| `e89e465` | chore: save untracked work before handoff | lockin/feature-workflow-runtime | ported (B3) | `docs/research/open-source-coding-agent-harness-alternatives.md` |
| `e5c3293` | fix: accept structured worker test evidence | lockin/feature-workflow-runtime | ported (B3) | cherry-picked as `f823a32` |
| `7e47194` | chore: save untracked work before handoff | plan/codex-native-harness | dropped | new OpenSpec proposal `adopt-native-multi-backend-runtime` (Codex App Server adapter, harness doctor): proposal-only, outside the seven changes in HANDOFF §4, and v4.1.0 is Claude-only; it stays on `plan/codex-native-harness` |
| `cb07c2a` | chore: save the project-picker custom Cog (fork-derived, unt | save/project-picker-extension | dropped (B4) | fork-derived project picker; superseded by `/launcher` |


Net result: one commit ported here (`65ab7b9` → `e3ce5c4`), three dropped with reasons
(`1d9d71d`, `7e47194`, `cb07c2a`), everything else already in the tree or ported by B2–B4.
No new build boxes needed.

Proof: `scripts/test-clean-env.sh tests/test_feature_workflow.py tests/test_plan_card.py
tests/test_run_helper.py` → 115 passed; the only change in this box is a docs cherry-pick.

## C1: stale checkboxes reconciled (commit: the openspec-only commit before this one)

Each change's `tasks.md` was compared with the code, its tests and the three wave manifests
(`.planning/2026-09-20-discord-agent-flow-program/wave-{1,2,3}-manifest.json`, which only
ever scheduled section-1 tasks). Ticked (code + test cited); everything else stays open:

| change | ticked | implementation | tests |
|---|---|---|---|
| discord-command-surface | 1.3 | `claude_discord/session_lifecycle.py` (`SessionLifecycleService`, `CloseAuthorization`) | `tests/test_session_lifecycle.py::TestCloseAuthorization`, `::TestCloseAnIdleSession`, `::TestCloseDuringAnActiveTurn`, `::TestReopenService` |
| discord-command-surface | 3.3 | `claude_discord/cogs/backend_command.py:294-383` (`_switch_autocomplete`: ✅ current marker, typed search, `backend|model` values), `claude_discord/model_catalog.py` (newest-first aliases, hidden models) | `tests/test_switch_command.py` (9 tests), `tests/test_model_catalog.py::test_alias_points_at_the_newest_model_of_its_family`, `::test_hidden_models_are_not_offered` |
| trusted-agent-handoffs | 1.3 | `claude_discord/database/models.py:150-239` (handoff tables, UNIQUE(task_id, recipient), UNIQUE event_id), `claude_discord/database/handoff_repo.py` | `tests/test_handoff_repository.py` (incl. `test_concurrent_duplicate_inserts_schedule_one_logical_task`, `test_result_and_outbox_row_are_written_together`) |
| shared-project-catalog | 1.3 | `claude_discord/project_catalog.py:608-1108` (`OwnerRegistry`, `CatalogResolver`, `parse_owner_phrase`) | `tests/test_project_catalog.py::TestOwnerRegistry`, `::TestParseOwnerPhrase`, `::TestRemoteOwnerResolution`, `::TestLocalOwnerResolution`, `::TestAmbiguousOwnerHandling` |
| discord-my-ai-setup | 1.3 | `claude_discord/ai_setup_collector.py` (`AdapterRegistry`, `InventoryCollector`, `merge_*`) | `tests/test_ai_setup_collector.py` (e.g. `test_verified_loading_beats_a_discovered_guess_for_the_same_harness`) |
| professional-harness-audit | 1.3 | `extensions/harness_audit/sources.py` + `sources.json` (`load_manifest`, fail-closed) | `tests/test_harness_audit_sources.py::test_expired_source_fails_closed`, `::test_non_official_url_fails_closed`, `::test_unknown_publisher_fails_closed` |
| parallel-gowork | 1.3 | `extensions/feature_workflow/run_state.py` (8 states, legal-transition map, atomic replace) | `tests/test_feature_run_state.py::TestTransitions`, `::TestRestartSafety` |
| parallel-gowork | 2.3 | `extensions/feature_workflow/briefing.py` | `tests/test_feature_briefing.py::TestExcludesTheParentTranscript`, `::TestRestrictions` |
| parallel-gowork | 4.1 | `extensions/feature_workflow/planning_models.py` (`SplitIdentity`, handoff + child/parent records) | `tests/test_planning_models.py::TestHandoffSerialization`, `::TestChildRecord`, `::TestBoundedText` |
| model-capacity-resilience | 2.1, 2.2 | `claude_discord/database/models.py:256-280` (`capacity_pending_turns`), `claude_discord/database/capacity_recovery_repo.py` (`CapacityRecoveryRepository`) | `tests/test_capacity_recovery_repo.py` (`test_conditional_claim_has_one_winner`, `test_accept_once_wins_over_late_duplicates`, `test_expiry_marks_only_unaccepted_pending_turns`, `test_restart_reload_lists_due_records_without_claiming`, migration tests) |

What the audit found still open (this is the D3–D9 work list; details per task are in
the reports summarised here):

- discord-command-surface: 2.1–2.5, 3.1, 3.2, 3.4, 4.1–4.4. `command_surface.py` and
  `session_lifecycle.py` are built and tested but imported by no cog yet.
- trusted-agent-handoffs: 2.1–2.4, 3.1–3.4, 4.1–4.5. A narrower slice
  (`claude_discord/handoff_{sender,inbox,messages,executor,return,status,triggers}.py`)
  implements the project-lookup use case end to end on top of the section-1 protocol;
  config/trust checks, authority scope, progress posts, reconnect scan, retry scheduling
  and the three-identity integration test are not there.
- shared-project-catalog: 2.1, 2.2, 3.1–3.3, 4.1–4.3, 5.1, 5.2. The catalog module is
  imported nowhere; launcher/API still use raw-path favorites and `CCDB_PROJECT_ROOTS`.
- discord-my-ai-setup: 2.1–2.3, 3.1–3.3, 4.1–4.4, 5.1, 5.2. Only the domain, redaction
  and collector exist (181 tests); no adapters, persistence, view or Settings entry.
- professional-harness-audit: 2.1–2.3, 3.1–3.3, 4.1–4.3, 5.1, 5.2. No discovery,
  collectors, rules, classify, parity, report, quarantine or CLI; no `__init__.py`.
- parallel-gowork: 2.1, 2.2, 3.1–3.5, 4.2–4.4, 5.1–5.3 (see C2 for what the Go Work
  upgrade plan covers).
- model-capacity-resilience: 2.3, 3.1–3.4, 4.1–4.3, 5.1–5.3. Classifier + repo exist;
  no restart loader, coordinator, fallback chains, surface notices or `/gowork` migration.

## C2: parallel-gowork ↔ Go Work upgrade overlap

Mapping written in `docs/plans/v4.1.0-decisions.md` (section C2). Net: 13 parallel-gowork
tasks are covered by T-tasks and are ticked when those land (D2); D9 builds only 2.1, 4.3,
4.4, the metadata half of 5.1, and 5.2. Proof: docs only, no test run needed.

## C3: trial-greeting, trial-word-count, folder-session-launcher

- `trial-greeting` and `trial-word-count`: fully implemented in
  `examples/feature_workflow_trial/` (`greeting.py`, `counter.py`, `cli.py`,
  `test_greeting.py` 6 cases, `test_counter.py` 5 cases, `test_integration.py` 3 cases;
  landed in `7596489`). The only non-code boxes (push to Drew's `voice-backup` remote,
  lead integration in a named thread) are satisfied by the commits being in this tree.
  Ticked every box and archived both with the OpenSpec CLI (v1.13.1 via `npx`):
  `openspec/changes/archive/2026-09-21-trial-greeting`, `…-trial-word-count`;
  their delta specs synced into `openspec/specs/`.
- `folder-session-launcher`: 17/20 boxes were already ticked and the code is present
  (`claude_discord/cogs/project_launcher.py`, `category_scope.py`). The 3 open boxes are
  live activation steps (activate on the Lenovo at an idle window, verify the panel on
  David/iMac, activate the bottom shortcut) — handled under D1, not here.

Proof:

```
uv run python -m unittest discover -s examples/feature_workflow_trial -p "test_*.py"
  → Ran 14 tests … OK
uv run ruff check examples/feature_workflow_trial → All checks passed!
uv run pyright examples/feature_workflow_trial → 0 errors
npx @fission-ai/openspec validate trial-greeting --strict → Change 'trial-greeting' is valid
npx @fission-ai/openspec validate trial-word-count --strict → Change 'trial-word-count' is valid
npx @fission-ai/openspec archive … --yes → archived as '2026-09-21-trial-greeting' / '2026-09-21-trial-word-count'
scripts/test-clean-env.sh tests/test_project_launcher.py tests/test_category_scope.py → 38 passed
```

## C4: `.planning/2026-09-10-land-everything/task_plan.md`

Every task annotated in place. 15 of 17 ticked as done / superseded by a v4.1.0 box /
Lenovo-only housekeeping (each line says which). Tasks 8 and 9 (Codex `model_context_window`
into the stream event; DSH context estimate) are genuinely unbuilt — `context_window` is only
set by the Claude parser (`claude_code_core/parser.py:262`) — and became D10a and D10b.

Proof: `scripts/test-clean-env.sh tests/test_deploy_recovery.py tests/test_thread_policy.py`
→ 10 passed (the two tests Tasks 6/7 were about); docs otherwise.

## D1: folder-session-launcher

The three open boxes were live-activation steps, not code: activation on the Lenovo is the
v4.1.0 switch-over itself (HANDOFF §8), David's bot is checked in E5, the iMac is outside this
build. Each box now carries that note. The delta spec's last requirement ("Keep control-center
controls reachable", from `a87b265`) had no scenario, which `validate --strict` rejects; added
the scenario the four shortcut tests already prove. Archived as
`openspec/changes/archive/2026-09-21-folder-session-launcher`; spec synced to
`openspec/specs/folder-session-launcher/`.

Proof:

```
scripts/test-clean-env.sh tests/test_project_launcher.py tests/test_category_scope.py → 38 passed
npx @fission-ai/openspec validate folder-session-launcher --strict → Change 'folder-session-launcher' is valid
npx @fission-ai/openspec archive folder-session-launcher --yes → archived as '2026-09-21-folder-session-launcher'
```

## D3: model-capacity-resilience

Built in worktree branch `worktree-agent-a542d63e86f8347ad` (from `release/v4.1.0` at
`df5491a`). Every task: failing test first, then code; one commit per task group. Gate before
each commit: `ruff check` + `ruff format` + `pyright` (0 errors) on `claude_discord/
claude_code_core/`, plus the change's Check line.

- **3.1** retry policy — `claude_code_core/capacity_policy.py` (`RecoveryPolicy.from_env`,
  `retry_delay`, `RecoveryTracker`, `RecoveryStatus.line/public_view`, `FallbackTarget`,
  `parse_fallback_chain`). Commit `cea5a10`. Proof: `scripts/test-clean-env.sh
  tests/test_capacity_policy.py -q -p no:cacheprovider` → 21 passed (was: collection error).
- **2.3 + 3.2** coordinator and restart loader — `claude_discord/capacity_recovery.py`
  (`CapacityRecoveryCoordinator.run_turn` with claim → attempt → classify → decide →
  schedule/accept-once; `CapacityRestartLoader.load_due` claims via the conditional
  update, so two loaders resume each turn exactly once). Commit `e4dd486`. Proof:
  `scripts/test-clean-env.sh tests/test_capacity_coordinator.py -q -p no:cacheprovider`
  → 16 passed (was: 16 failed/errors); Check line → 281 passed.
- **interactive integration** — `_run_helper.run_claude_with_config` splits into
  `_run_one_attempt` (relay admission per attempt) + `_run_recovered_turn`
  (`configure_capacity_recovery`, `_RecoveryPresenter`: one `open_activity` edited in place,
  registry state `recovering`); `RunConfig.recovery_turn_key/recovery_claim_token/
  fallback_chain/capacity_recovery`; `EventProcessor` leaves classified capacity errors to
  the recovery line; `ClaudeChatCog.resume_capacity_turn` + loader on first `on_ready`;
  `setup_bridge` installs the coordinator with `stores.capacity` (zero-config; `.env.example`
  documents `CCDB_CAPACITY_*`). Commit `5c9c699`. Proof: `scripts/test-clean-env.sh
  tests/test_capacity_integration.py -q -p no:cacheprovider` → 12 passed (was 11 failed +
  11 errors); `tests/test_setup.py tests/test_run_helper.py` → 94 passed.
- **3.3 + 3.4 + 5.2** `/gowork` — `TaskLoopCog._limit_hit` classifies the message; retryable
  outcomes go through `RecoveryTracker` (one `-#` status line edited per build, the build's
  `fallback` is a one-entry `task` chain), everything else keeps the switch-or-wait
  question. `claude_code_core.task_loop.usage_limit_message` now reads the shared classifier
  (`_LIMIT_RE`/`_LIMIT_IN_REPLY_RE` removed). Commit `abb5d30`. Proof:
  `scripts/test-clean-env.sh tests/test_task_loop_capacity.py -q -p no:cacheprovider` → 9
  passed (was 7 failed); `tests/test_task_loop.py tests/test_task_loop_cog.py` green in the
  368-passed run of the Check line + neighbours.
- **4.1–4.3 + 5.1** — `tests/test_capacity_notices.py` (ten phases, distinct lines, category
  named); `/api/sessions` `capacity.recovering` + `capacity.recovery` (public view only,
  `claude_discord/ext/api_server.py`); log-capture test proves no prompt/diagnostic in logs.
  5.1's restart / late-result / exhausted / authorized-fallback / no-authority cases:
  `tests/test_capacity_integration.py`, `tests/test_capacity_coordinator.py`. Commit
  `f4d92a5`. Proof: `scripts/test-clean-env.sh tests/test_capacity_coordinator.py
  tests/test_capacity_notices.py tests/test_session_observability.py -q` → 46 passed.
- **5.3** — `uv run ruff check claude_discord tests` → All checks passed; `uv run pyright
  claude_discord` → 0 errors; `uv run ruff format --check claude_discord tests` → one
  pre-existing unformatted file unrelated to this change, `tests/test_main_config.py`
  (last touched in `309a85b`), left alone; full suite `scripts/test-clean-env.sh -p
  no:cacheprovider` → **4757 passed, 35 skipped in 204.67s**; `OPENSPEC_TELEMETRY=0 npx
  @fission-ai/openspec validate model-capacity-resilience --strict` → valid.

Decisions recorded in `docs/plans/v4.1.0-decisions.md` § D3.

## D10a: Codex context window for the start-fresh nudge

`claude_code_core/codex_runner.py`: `parse_codex_line` reads `token_count`
(`info.model_context_window`, `info.last_token_usage`) into a silent SYSTEM event;
`_read_stream` folds the last one onto `turn.completed` (`_fold_token_count`), so
`event_processor.py` persists `context_window`/`context_used` for Codex threads (end-to-end
test in `tests/test_event_processor.py::TestContextStatsPersistence`). Commit `a3cdfc5`.
Proof: `scripts/test-clean-env.sh tests/test_codex_runner.py tests/test_event_processor.py -q
-p no:cacheprovider` → 152 passed (was 2 failed in `TestContextWindowFromTokenCount`).

## D10b: DSH context estimate

`claude_code_core/dsh_backend.py`: `estimate_tokens` (chars/4, rounded up),
`context_window_for` (prefix table + `CCDB_DSH_CONTEXT_WINDOW`), per-session accumulation in
`_SESSION_ESTIMATES`; the closing event carries `context_window`, `input_tokens` and the new
`StreamEvent.context_estimated`. `claude_code_core.context_nudge.context_label()` appends
"(estimate)" for `dsh` records and is used by the Done notice (`_completion_fields`),
`/context`, the start-fresh question and the rewind note. Commit `b582ba8`. Proof:
`scripts/test-clean-env.sh tests/test_dsh_backend.py tests/test_session_manage.py
tests/test_context_nudge_cog.py tests/test_context_nudge.py tests/test_event_processor.py
tests/test_claude_chat.py -q -p no:cacheprovider` → 309 passed (was 7 failed).

## D4: discord-command-surface

Worktree: `.claude/worktrees/agent-a9b269ba821c487c1` (branch `worktree-agent-a9b269ba821c487c1`, reset to `release/v4.1.0`).
Check line fixed first: `tests/test_backend_command.py` never existed; the `/switch` tests are `tests/test_switch_command.py` (decisions file).

### 2.1 control row

`ControlRowView` (persistent, custom ids `ccdb:control:{new,sessions,settings}:v1`, labels from `CONTROL_CENTER_BUTTONS`) replaces `LauncherView` on the bottom shortcut; `status_block()` (computer name, active sessions, default backend/model read from `BackendSettings` — no model turn) sits above the buttons; `refresh_shortcut()` saves the new id before deleting the previous bot-owned row; `ensure_control_row()` on reconnect edits the saved row or posts one when it is gone; the listener ignores `ccdb:control:` messages. The pinned panel keeps the old buttons until 4.4.

```
scripts/test-clean-env.sh tests/test_project_launcher.py tests/test_category_scope.py → 45 passed
Check → 268 passed
```

Commit for 2.1: `ea76200`.

### 2.2 New session: Favorites / Recent / Browse

`show_new_session()` posts an ephemeral `NewSessionMenu` (Favorites, Recent, Browse); Favorites and Recent open a `FolderPick` select whose choice calls `new_session()` directly (no browser hop), Browse opens the existing `FolderBrowser`. `new_session()` still writes `repo.save(thread_id, "", working_dir=path)` and now names the default model in the thread notice via `default_model()` (BackendSettings, no CLI). Tests assert `chat.spawn_session` / `chat._run_claude` are never awaited, a missing recent folder is skipped, and one pick creates one thread.

```
scripts/test-clean-env.sh tests/test_project_launcher.py → 42 passed
Check → 274 passed
```

Commit for 2.2: `d184fe0`.

### 2.3 Create and Clone under approved roots

New `claude_discord/project_creation.py`: `ProjectRoots` (from `CCDB_PROJECT_ROOTS`, comma or path-separator list, cwd fallback; only real absolute folders kept), `validate_name` / `resolve_destination` (no separators, `..`, absolute, hidden, `-`-prefixed or Windows-reserved names; case-insensitive collision; result re-checked to lie beneath the root), `parse_repository` (GitHub `owner/repo` or `https://` only — ssh, file, local paths and flag-shaped strings refused), `create_project` (exclusive mkdir), `clone_project` (`git clone -- <url> <dest>` via `create_subprocess_exec`, no shell, `DISCORD_*`/`CCDB_*` stripped, `GIT_TERMINAL_PROMPT=0`; a failed/timed-out clone removes only the folder the attempt created and starts nothing). Launcher: Create and Clone buttons open modals; `create_and_start()` is the one thin call and ends in the same `_start_idle_thread()` every other choice uses.

```
scripts/test-clean-env.sh tests/test_project_launcher.py tests/test_project_creation.py → 85 passed
Check (+ tests/test_project_creation.py) → 317 passed
```

Commit for 2.3: `6d006ae`.

### 2.4 Sessions browser

New `claude_discord/discord_ui/session_browser.py`: `SessionBrowser.find(query)` walks `repo.list_all()` newest-first (no query added to the repository — `search()` already exists but cannot see thread titles), resolves each record through the caller's visibility resolver (the launcher's `visible_thread`, which also rejects other computers' channels), filters title/summary/folder case-insensitively, keeps closed records with a `closed` marker, and stops at 25. `SessionBrowserView`: one select + Open / New in same folder / Close / Search (modal); each button calls exactly one `SessionActions` method. Launcher: `show_sessions(query)`, `open_session` (lifecycle reopen when wired, else unarchive; links the original thread, no clone, no save), `new_in_same_folder` → `new_session`, `close_from_sessions` → lifecycle or a safe decline; `close_outcome_text()` added to `session_lifecycle.py` so every frontend says the same sentence.

```
scripts/test-clean-env.sh tests/test_project_launcher.py tests/test_session_browser.py tests/test_session_lifecycle.py → 138 passed
Check (+ tests/test_session_browser.py) → 299 passed
```

Commit for 2.4: `4e43004`.

### 2.5 Settings home

New `claude_discord/discord_ui/settings_home.py`: `SupportedFeatures.detect()` (claude always; codex when its CLI is on PATH; local/dsh/agui when `CCDB_LOCAL_BASE_URL` / `DSH_ROUTES`|`DSH_MODELS_PATH` / `CCDB_AGUI_URL` are set; `CCDB_SUPPORTED_HARNESSES` and `CCDB_SUBSCRIPTION_FEATURES` override), `SettingsEntry` (harness/feature requirements, optional `open` coroutine, otherwise a `/command` hint), `SettingsHome.visible()` drops unsupported entries (absent, not disabled), `render()` → text + `SettingsHomeView` with one button per openable entry. Extension point for My AI Setup: `register_entry(SettingsEntry(...))` (module registry, feeds every later home) or `components.settings_home.add(entry)` / `ProjectLauncherCog.settings_home.add(entry)` (live home); entries replace by key. `BridgeComponents.settings_home` added. Launcher `show_settings()` renders it ephemerally; test asserts no `spawn_session`/`_run_claude`.

```
scripts/test-clean-env.sh tests/test_settings_home.py → 11 passed
Check (+ tests/test_settings_home.py) → 299 passed
```

Commit for 2.5: `006f169`.

### 3.1 session services

`ClaudeChatCog` gains one interaction-free service per action — `stop_turn`, `compact_thread`, `run_goal` + `goal_label`, `clear_thread` (keeps the folder; decisions file), `rewind_view` (None without history), `fork_thread` — and `/stop`, `/compact`, `/goal`, `/clear`, `/rewind`, `/fork` now delegate to them; `session_manage.context_embed(record, thread_name)` backs `/context`. The handoff region (~370–560) of `claude_chat.py` is untouched. Existing command tests stay green; `TestSessionServices` (9) and `TestContextService` (2) added.

```
scripts/test-clean-env.sh tests/test_claude_chat.py tests/test_session_manage.py tests/test_rewind_fork.py tests/test_rewind_jsonl.py tests/test_task_loop_cog.py → green (417 with the Check files)
Check → 299 passed
```

Commit for 3.1: `ae14592` (its Check line above should read: the 6 Check files + `tests/test_task_loop_cog.py` → 417 passed; Check alone → 299 passed).

### 3.2 `/session` action view

New `claude_discord/discord_ui/session_actions.py`: `SessionActionsView` (Fork, Rewind, Compact, Clear, Context, Goal — personal, ephemeral), `ConfirmView` (Clear runs once after an explicit confirm; Cancel changes nothing), `GoalModal` (blank = show, `clear` = remove). New `claude_discord/cogs/surface_commands.py`: `locate()` classifies an interaction through `CommandSurface` (thread bound to a record = managed session), `SurfaceCommandsCog` registers `/session` (fails closed elsewhere with the coordinator's correction, no state change), and `ChatSessionActions` maps each button to exactly one 3.1 service (`fork_thread`, `rewind_view`/`clear_thread`, `compact_thread`, `clear_thread`, `context_embed`, `run_goal`). Registered in `setup_bridge()` next to `ClaudeChatCog`; `session` added to `_HELP_CATEGORY`.

```
scripts/test-clean-env.sh tests/test_surface_commands.py tests/test_session_actions.py tests/test_help_sync.py tests/test_setup.py → 53 passed
Check → 299 passed
```

Commit for 3.2: `c44aa99`.

### 3.4 `/close` and Sessions Open/Close on the lifecycle service

New `claude_discord/lifecycle_adapters.py`: `ChatTurnActivity` (a turn is active while `ClaudeChatCog._active_runners` holds the thread; `wait_until_idle` awaits its task), `DiscordThreadSurface` (archive/unarchive by `thread.edit(archived=...)` — never `locked`, never delete; posts the stored wrap-up *before* archiving because a later message would un-archive), `build_lifecycle_service()`. `setup_bridge()` builds one service and hands it to `ClaudeChatCog.lifecycle`, `SurfaceCommandsCog(lifecycle=)` and `ProjectLauncherCog(lifecycle=)`. `/close` (in `surface_commands.py`, session threads only, ephemeral reply) calls `lifecycle.close(thread_id, CloseAuthorization.from_interaction(user))`; `ClaudeChatCog._run_claude`'s `finally` calls `_complete_pending_close()` after releasing the slot; `on_ready` calls `reconcile_pending_closes()` before resumes. Sessions Open → `lifecycle.reopen` (2.4 already routed it), Close → `lifecycle.close`. The old destructive `close_session` helper is untouched and used only by the task loop.

```
scripts/test-clean-env.sh tests/test_lifecycle_wiring.py (idle, active→finalized, duplicate, wrong place, restart reconcile, reopen same/other harness) tests/test_surface_commands.py tests/test_help_sync.py tests/test_setup.py tests/test_task_loop_cog.py → 175 passed
Check → 304 passed
```

Commit for 3.4: `cd7b140`.

### 4.1 registration + location-aware `/help`

`SurfaceCommandsCog` now registers `/new`, `/settings`, `/session`, `/close` (with `launcher=` for the control-center flows); `/sessions` keeps its `SessionManageCog` registration, gains `query`, and routes to `surface_cog.open_sessions` when attached (decisions file). `command_surface.help_sections(location)` (pure) gives the Buttons/Commands lists per location; `ClaudeChatCog.help_command` renders them ephemerally when `command_surface` is set and the place is a control center or managed session, else the full legacy embed. Every command outside its place sends the coordinator's correction and changes nothing. `_HELP_CATEGORY` covers `new`, `settings`, `session`, `close`; `tests/test_help_sync.py::test_final_surface_and_legacy_commands_coexist_until_retirement` proves the eight final names and the legacy names are registered together.

```
Check → 310 passed
scripts/test-clean-env.sh tests/test_surface_commands.py tests/test_help_sync.py tests/test_setup.py tests/test_sync_command.py tests/test_lifecycle_wiring.py → 74 passed
```

Commit for 4.1: `9a16c8d`.

### 4.2 verification gate

`uv run ruff check claude_discord/ tests/` → All checks passed; `uv run ruff format --check claude_discord/ claude_code_core/` → 181 files already formatted (two of my test files were reformatted; `tests/test_main_config.py`, which ruff also wanted to reformat, is out of scope and was left as it was); `uv run pyright claude_discord/` → 0 errors, 0 warnings. Security checklist for the Cog/subprocess changes: the only new subprocess is `project_creation.run_git` (`create_subprocess_exec`, argv `git clone -- <url> <dest>`, no shell, no interpolation, folder name and repository validated first, `DISCORD_*`/`CCDB_*` stripped, `GIT_TERMINAL_PROMPT=0`, killed on timeout, attempt-owned cleanup); thread ids come from the bot's own select options and are cast with `int()`; no secrets or internal paths in messages or logs; every command outside its location changes nothing.

```
Check → 310 passed
```

Commit for 4.2: `fe42873`.

### 4.3 Discord try-out — deferred

Deferred to the release switch-over (E5). It needs `make dev-on` on the live bot and Drew pressing the three flows in Discord; nothing offline can stand in for it. Box left open; the tasks.md line carries the note.

### 4.4 retirement — code part behind a switch, box open

`command_surface.retirement_enabled()` / `retire_superseded_commands(tree)` (`CCDB_RETIRE_SUPERSEDED_COMMANDS`, off by default; `CCDB_RETIREMENT_KEEP` spares instance commands) removes every top-level registration outside the final eight — registration only, applied at the end of `setup_bridge()`; `ProjectLauncherCog.panel_view()` swaps the pinned panel's old Favorite folders / New session / Resume buttons for the control row under the same switch. Off, nothing changes (proved). The box stays open until 4.3 is recorded and the switch is turned on; see the decisions file.

```
scripts/test-clean-env.sh tests/test_command_retirement.py tests/test_project_launcher.py tests/test_setup.py tests/test_command_surface.py → 113 passed
```

Commit for 4.3/4.4: `b500f68`.

### D4 finish

Boxes ticked: 2.1–2.5, 3.1, 3.2, 3.4, 4.1, 4.2 (with 1.1–1.3, 3.3 from before). Open: 4.3 (deferred to E5), 4.4 (switch in place, flipped after 4.3). Extension point for D6 (My AI Setup): `claude_discord/discord_ui/settings_home.py` — `register_entry(SettingsEntry("ai-setup", "My AI Setup", ..., open=coro))` or `components.settings_home.add(entry)`.

```
scripts/test-clean-env.sh -p no:cacheprovider -rf → 4830 passed, 35 skipped, 18 warnings in 183.79s (repeated: 4830 passed, 35 skipped in 185.07s; a first run without -rf reported 1 failed / 4829 passed that two reruns did not reproduce and whose name the -q summary did not print)
npx @fission-ai/openspec validate discord-command-surface --strict → Change 'discord-command-surface' is valid
```

## D8: professional-harness-audit

Built on branch `worktree-agent-aff0ed8998c422098` (from `release/v4.1.0` @ `df5491a`). Every
test run below goes through `bash scripts/test-clean-env.sh <paths> -q -p no:cacheprovider`;
"the check command" is line 1 of `tasks.md` (`pytest tests/test_harness_audit_*.py -q`) run the
same way.

### Code-review fixes in `redaction.py` (commit `5c059eb`)

- `_is_safe_value` no longer treats a number as safe: `DB_PASSWORD=48213907` is redacted and the
  fail-closed re-scan refuses it (`test_a_purely_numeric_secret_value_is_still_a_secret`, 3 cases).
- The credential-header value stops at a JSON-escaped `\n`/`\r`, so `serialize_bundle` accepts its
  own `[redacted]\nHost: x` output (`test_credential_header_value_stops_at_a_json_escaped_newline`).
- RED: 4 failed, 40 passed → GREEN: check command → 173 passed.

### 2.1 discovery (`extensions/harness_audit/discovery.py`, `tests/test_harness_audit_discovery.py`)

- `DiscoveryRoots` (no defaults; roots must exist) + `discover()` walk a fake home and describe
  global/project instructions, memory links, skills, commands, agents, plugins, connectors
  (`~/.claude.json`, `.mcp.json`, `mcp_servers`), hooks (from settings) and native settings with
  a `~`-relative path, the resolved symlink target, permissions, exact size + labeled token
  estimate, salted hash, secret-match redaction status, content signals (frontmatter bytes,
  declared name, referenced project names, import count) and value-free `SettingsFacts`.
- Excludes any link that resolves outside the approved roots (reason recorded), never writes,
  never carries a setting value (`test_settings_facts_carry_names_and_shapes_but_never_values`
  asserts the fake key, a numeric password, `vim` and `gh-mcp` are absent from the serialized
  result; `test_a_link_outside_the_approved_roots_is_excluded_not_followed`;
  `test_discovery_never_writes_and_reads_only_the_roots_it_was_given`).
- RED: `ModuleNotFoundError` (10 tests) → GREEN: 10 passed; check command → 183 passed.

### 2.2 Claude collector (`extensions/harness_audit/claude_collector.py`, `collection.py`, `tests/test_harness_audit_claude.py`)

- `collect_claude(discovery, machine=, invocation=, manifest=, collected_at=)` resolves each
  discovered Claude file into an `InventoryItem`: `loaded` only with a session transcript for the
  invocation cwd on a CLI version the pinned guidance covers (`test_session_transcript_plus_invocation_proves_loaded`);
  `configured` for a file in a read location without session proof and for every MCP server
  (nothing proves a server connected); `installed-only` for commands/subagents (read on invoke),
  scopes excluded by `--setting-sources`, `--strict-mcp-config`, disabled servers and a project
  `AGENTS.md` Claude does not read; `unknown` for unparseable settings and an uncovered version
  (`1.0.0` is named in `missing_evidence`). Symlinked memory carries both sources with the canonical
  target; `--append-system-prompt` is a `loaded` bot addition whose body is withheld as a
  `PrivateBody`; `--model`/`--permission-mode` are recorded as session settings marked "excluded
  from cleanup"; environment values never exist (names only, `[redacted]`).
- `collection.py` holds the records both collectors share (`Invocation`, `SessionObservation`,
  `CollectionResult`); `read_claude_sessions` reads only `cwd`/`version` (`test_read_claude_sessions_keeps_metadata_only`).
- An autouse fixture patches `subprocess.run/Popen` and `asyncio.create_subprocess_exec` to raise,
  so the whole file proves no CLI or model call happens.
- RED: `ModuleNotFoundError` (13 tests) → GREEN: 13 passed; check command → 196 passed.

### 2.3 Codex collector (`extensions/harness_audit/codex_collector.py`, `tests/test_harness_audit_codex.py`)

- `collect_codex(discovery, machine=, invocation=, manifest=, collected_at=, salt=)`: `~/.codex/AGENTS.md`
  is `loaded` by `rollout-instructions-hash-match` when the rollout's `session_meta.instructions`
  hash (salted like discovery) equals the file hash; project `AGENTS.md`/`config.toml` are `loaded`
  when a rollout for the invocation cwd exists on a covered version; skills from both
  `~/.codex/skills` and `~/.claude/skills` are `loaded` when the developer turn names them, else
  `configured`; custom prompts and `enabled = false` servers are `installed-only`; an unparseable
  `config.toml` and a version outside the manifest range (`0.100.0`) are `unknown`.
- Settings: one item per notable key with every source (`-c` override at precedence 1,
  `config.toml` at 3), label shows both values; model/reasoning items carry "excluded from cleanup"
  and `test_model_and_reasoning_settings_are_recorded_and_left_untouched` hashes `config.toml`
  before/after. `-c developer_instructions=…` is a `loaded` bot addition with the body withheld;
  any other `-c` value is `[withheld]` (`test_unknown_override_values_are_withheld`).
- Same autouse no-subprocess fixture as 2.2. RED: `ModuleNotFoundError` (13 tests) → GREEN:
  13 passed; check command → 209 passed.

### 3.1 rules (`extensions/harness_audit/rules.py`, `tests/test_harness_audit_rules.py`)

- `run_rules(results, manifest=)` applies the eight checks: duplication by salted sha256 across
  targets (two maintained copies → FAIL with `duplicated_bytes`, a symlink pair to one canonical
  file → PASS), project-content-is-global by project-name token match (recommends a move),
  size with `SizeMeasurement.label` (exact bytes/characters + "estimated … not provider billing
  data"; > `LARGE_CONTEXT_BYTES` fails; a per-target session-start total), permissions (mode
  `0666` on a hook-bearing settings file → HIGH; `unknown` mode → UNKNOWN with missing evidence;
  `bypassPermissions` / `danger-full-access` → FAIL), precedence (`-c` override wins; model and
  reasoning keys are never dead-configuration targets), dead configuration (shadowed global
  command, undocumented hook event `OnSave`, scope excluded by `--setting-sources`, disabled
  server) and load behaviour (unknown items → UNKNOWN with `missing_evidence`; passes aggregated
  per target and level, installed-only "not counted").
- `test_every_finding_cites_local_evidence_and_official_guidance_when_required` checks every
  finding has a `source_reference` and that vendor-backed failures carry a dated citation;
  `test_rules_are_deterministic_and_never_carry_content` pins byte-stable output.
- Discovery change folded in: `modes` may declare `None` and Windows reports `unknown` (POSIX bits
  are meaningless there) so the permission check can never fake a pass or a failure.
- RED: `ModuleNotFoundError` (13 tests) → GREEN: 13 passed; check command → 222 passed.

### 3.2 classify (`extensions/harness_audit/classify.py`, `tests/test_harness_audit_classify.py`)

- `classify(results, findings)` → exactly one `ClassificationRecord` per item id across every
  target that has it (`test_every_item_gets_exactly_one_verdict_with_reason_evidence_and_action`).
  `choose_verdict()` is the pure decision over `VERDICT_PRECEDENCE` (dead-configuration →
  project-content-is-global/scope → size → duplication/permissions/precedence/load-behavior):
  undocumented hook event and `enabled = false` server → Remove with a quarantine action; shadowed
  command → Fix; project-naming global skill → Move to project (`proposed_scope = project`); a
  large session-start file → Load only when needed with a skill activation boundary; two
  maintained copies → Fix naming the canonical source; model/reasoning → Keep ("excluded").
- Incomplete evidence can never produce Remove: `test_choose_verdict_never_returns_remove_on_incomplete_evidence`
  enumerates all 256 failing-check subsets × removable with `incomplete=True`;
  `test_a_remove_candidate_with_unknown_evidence_becomes_fix_with_uncertainty` runs the real
  pipeline with an uncovered CLI version and an `OnSave` hook → Fix + `uncertainty`.
- RED: `ModuleNotFoundError` (12 tests) → GREEN: 12 passed; check command → 234 passed.

### 3.3 parity (`extensions/harness_audit/parity.py`, `tests/test_harness_audit_parity.py`)

- `portable_identity(item)` names what an item contributes independent of harness format
  (`global-instructions`, `project-instructions`, `skill:<name>`, `bot-addition`, and adapters
  `setting:<key>`, `hook:…`, `connector:<name>`); `check_parity(inventories, overlays=)` compares
  portable identities across harnesses on one machine and everything across machines for one
  harness, by content hash. Equal → `aligned`; differing/missing → `drift` with a `parity`
  finding ("keep one canonical source … smallest necessary adapters … migration risk") unless a
  declared `MachineOverlay` exception covers it; model/reasoning differences are always a
  `model-availability` exception ("does not recommend fabricating an unavailable option"); a
  feature only one harness has is a `harness-format` adapter; a missing target yields `unknown`
  results and an UNKNOWN finding, never drift (`test_a_missing_target_is_a_gap_and_never_a_parity_conclusion`).
- `AuditCheck.PARITY` added to the schema (additive; see decisions).
- The four-target fixture (DrewAI + a sanitized iMac with a smaller subscription and no GitHub
  MCP) proves: global rules aligned on all four targets; subscription/installed-tool exceptions
  are exceptions with the declared id; the undeclared missing tool is drift; the real drift in
  the fixture (`~/.codex/skills/release` is a directory Claude never scans) is reported with the
  "link the canonical source" recommendation.
- RED: `ModuleNotFoundError` (9 tests) → GREEN: 9 passed; check command → 243 passed.

### 4.1 report (`extensions/harness_audit/report.py`, `tests/test_harness_audit_report.py`)

- `build_report(bundles, parity=, generated_at=)` combines one redacted bundle per machine (so
  only boundary-safe evidence can appear) and turns every approved target without an inventory
  into a `CoverageGap`; `render_json` (fixed key order) and `render_text` (Coverage, Inventory
  per target with `[loaded]`/`[configured]`/`[installed-only]`/`[unknown]` and "not counted
  toward effective context", Token impact with `SizeMeasurement.label`, Findings with
  `evidence:` lines and `source: … (retrieved …)`, Verdicts, Parity, Machine exceptions).
- Four-target fixture (DrewAI + iMac bundles round-tripped through `serialize_bundle`) renders
  `COMPLETE (4/4 targets)` with an inventory, token-impact entry and verdict set per target
  (`item_ids == verdict_ids`); one bundle renders `PARTIAL (2/4 targets)` naming `imac/claude`
  and `imac/codex` as coverage gaps with no verdict for them; a machine bundle lacking one
  harness yields a gap for that harness. No fixture secret, private body or file content
  appears in either rendering; rendering is byte-deterministic across two temp homes (transcript
  evidence now cites the file name, not the slug that encodes an absolute path).
- RED: `ModuleNotFoundError` (8 tests) → GREEN: 8 passed; check command → 251 passed.

### 4.2 quarantine (`extensions/harness_audit/quarantine.py`, `tests/test_harness_audit_quarantine.py`)

- `plan_quarantine(classifications, paths, quarantine_dir=, machine=, created_at=)` writes
  nothing (`test_planning_writes_nothing`) and records original path, sha256, disabled location,
  verdict, affected targets and rollback action for Remove/Move/Load-on-demand file items; a
  hook, server or setting key is `skipped` with "edit … under version control"; one file read by
  both harnesses is one entry (`also_item_ids`). `save`/`load` round-trip (`schema_version` 1).
- `apply_quarantine` verifies every hash and aborts before any move on a mismatch
  (`test_hash_mismatch_aborts_before_anything_moves`: tree unchanged, no quarantine dir), moves
  files (never deletes: `test_apply_moves_files_reversibly_and_never_deletes` compares the multiset
  of hashes on disk before/after) and refuses a second apply. `rollback_quarantine` restores every
  file by hash and refuses to overwrite a recreated original.
- `removal_eligibility` keeps Remove ineligible until applied, not rolled back, and every affected
  target is verified `True` (`test_remove_is_ineligible_until_every_affected_target_passes`: an
  unverified `drewai/codex` blocks it); non-Remove entries are never eligible. `target_passes`
  blocks on any HIGH failure or UNKNOWN; `reclassify_after_rollback` turns a restored item into
  Keep with the reason recorded.
- RED: `ModuleNotFoundError` (12 tests) → GREEN: 12 passed; check command → 263 passed.

### 4.3 CLI (`extensions/harness_audit/cli.py`, `extensions/harness_audit/__init__.py`, `tests/test_harness_audit_cli.py`)

- `python -m extensions.harness_audit.cli` with `audit` (writes only `--output`: `bundle-<m>.json`
  serialized through the fail-closed redactor, `paths-<m>.json`, `report.txt/json`), `export`,
  `import` (re-verifies; a tampered bundle with a key is refused and never lands), `compare`
  (`--overlay` files → declared exceptions), `quarantine plan|apply|rollback`, `verify` (re-runs
  the checks, writes `verify-<id>.json` with per-target pass/fail and removal eligibility) and
  `rollback`. Roots are explicit (`--home`, `--claude-home`, `--codex-home`, `--project`,
  `--declare` links/modes, `--invocation-claude/-codex`, `--salt`, `--sources`); a missing home is
  refused, never guessed. Exit 1 usage, 2 refused (schema/redaction/manifest/hash), 0 ok.
- `test_audit_is_read_only_and_writes_only_to_the_output_directory` hashes the whole fixture tree
  before/after; `test_no_command_exposes_a_model_effort_routing_or_subscription_option` walks
  every argparse action; `test_every_command_leaves_model_and_effort_settings_untouched` runs all
  seven commands and re-hashes `config.toml`/`settings.json`;
  `test_export_import_and_compare_produce_a_complete_four_target_report` → `COMPLETE (4/4 targets)`.
  An autouse fixture refuses any subprocess and pins the clock/today.
- `__init__.py` exports the public API; `uv run python -m extensions.harness_audit.cli --help`
  prints usage.
- RED: `ModuleNotFoundError` (13 tests) → GREEN: 13 passed; check command → 276 passed.

### 5.1 fixtures + operator doc (`tests/fixtures/harness_audit/`, `docs/harness-configuration-audit.md`, `tests/test_harness_audit_dry_run.py`)

- `tests/fixtures/harness_audit/{drewai,imac}/` are sanitized trees (canonical `~/AGENTS.md`
  with declared links, settings with a fake key, skills in both homes, a project with
  `CLAUDE.md`+`AGENTS.md`, a Claude transcript and a Codex rollout, invocation records, and the
  iMac `overlay.json` declaring subscription and installed-tool exceptions). Absolute paths are
  the `__HOME__` token; `materialize_fixture()` resolves it into a temp copy;
  `generate.py` regenerates the trees. `test_checked_in_fixtures_carry_no_real_path_and_no_real_secret_shape`
  greps every fixture file.
- `docs/harness-configuration-audit.md` documents the invocation record, the four steps (audit →
  export/import → compare → quarantine plan/apply/verify/rollback), `--declare` for Windows, the
  shared salt, overlays, how to read the report, removal eligibility and the manifest refresh rule.
- `test_documented_dry_run_changes_nothing_outside_the_output_directory` runs steps 1–4 exactly
  as documented on the materialized fixtures and asserts every file outside the output
  directories is byte-identical, no quarantine dir exists, the report is `COMPLETE (4/4 targets)`
  with `imac-subscription`/`imac-no-github-mcp` as exceptions, and no secret/body is present.
- RED: `ImportError` (`materialize_fixture`) → GREEN: 2 passed; check command → 278 passed.

### 5.2 integrated verification (no source files)

Commands run from the worktree root after 5.1 (all output verbatim):

```
uv run ruff check extensions/harness_audit tests/test_harness_audit_*.py → All checks passed!
uv run ruff format --check extensions/harness_audit tests/test_harness_audit_*.py → 27 files already formatted
uv run pyright extensions/harness_audit → 0 errors, 0 warnings, 0 informations
bash scripts/test-clean-env.sh tests/test_harness_audit_*.py -q -p no:cacheprovider → 278 passed
bash scripts/test-clean-env.sh -p no:cacheprovider → 4780 passed, 35 skipped in 203.88s
npx -y @fission-ai/openspec@latest validate professional-harness-audit --strict → Change 'professional-harness-audit' is valid
```

Security checklist (`.agents/skills/security-audit/SKILL.md`) applied to `extensions/harness_audit/`:

- Subprocess safety: `grep -rn "shell=True\|subprocess\|os.system\|eval(\|exec(" extensions/harness_audit/`
  matches only two prose strings ("present in the subprocess environment"); the package spawns
  nothing. Every collector/CLI test file patches `subprocess.run/Popen` and
  `asyncio.create_subprocess_exec` to raise.
- Environment: `grep -rn "os.environ\|getenv"` matches only a docstring; environment names come
  from the invocation record and values are never read (`redact_environment`).
- Secrets: bundles are serialized through `serialize_bundle`, which re-scans and refuses;
  `import` re-verifies; the two review holes (numeric password, escaped-newline header) are closed
  in commit `5c059eb`. Fixture keys are `sk-ant-api03-FAKE…` only; `.env` is gitignored.
- Input validation: `--machine`/`--harness`/`--verdict` are argparse choices; roots must be
  directories; invocation/overlay/manifest JSON go through `from_dict` validators that raise
  `SchemaError` (exit 2); paths written are confined to `--output`/`--to`/the quarantine dir.
- Error handling: fail-closed conditions exit 2 with a one-line reason; hash mismatch aborts
  before any move; a failed move is undone.
- No new dependency: `tomllib`, `json`, `hashlib`, `shutil`, `argparse` only.

No real quarantine has been proposed; the recorded commands above are the precondition for one.
