# v4.1.0 decisions made without Drew

One line each: question → what was chosen → why. Only for genuinely ambiguous specs.

- B1: `examples/ebibot/cogs/workdir_command.py` on `save/local-main-2026-09-21` is a git symlink to the absolute path `/home/drewp/main-projects/.../extensions/project_picker/workdir_command.py` (broken on any other machine; on Windows it checks out as a one-line text file that breaks `test_example_cogs_load` and `test_thread_policy`) → dropped the link, kept `extensions/project_picker/workdir_command.py` → B4 decides which picker implementation survives; nothing is lost.
- B4: three folder pickers after B1 — the framework launcher (`claude_discord/cogs/project_launcher.py`: `/launcher`, Favorites / New session / Resume / Browse, backed by `project_catalog.py`), `examples/project_creation/` (`/new`: Recent / All projects / Create project incl. GitHub clone — David's instance runs this), and the fork-derived `extensions/project_picker/` (`/cd`, `/cdnew` autocomplete over `CCDB_PROJECT_ROOTS`) → deleted `extensions/project_picker/`; kept the other two → it is the only one nothing loads (`CUSTOM_COGS_DIR` never pointed at it, per `docs/AUDIT-2026-09-10.md`), its second file was another broken absolute symlink, and its tests sat outside CI's `testpaths`; `/launcher` covers its use case (pick a folder → new session) with browse instead of autocomplete.
- B5: `7e47194` on `plan/codex-native-harness` adds a new OpenSpec change `adopt-native-multi-backend-runtime` (Codex App Server adapter, parity suite, harness doctor) → not brought into v4.1.0 → it is a proposal with no code, outside the seven changes HANDOFF §4 scopes, and a Codex-native runtime conflicts with the Claude-only rule; it stays on its branch for a later release.
- B5: `1d9d71d` (`.gowork-catchup.md`, a Go Work v3 status note) → dropped → the Go Work upgrade plan and its progress file are the live record of that state.

## C2: `openspec/changes/parallel-gowork` vs `docs/plans/gowork-upgrade-execution-plan.md`

Both build parallel Go Work on `extensions/feature_workflow/` + the `/gowork` loop. Rule: the
Go Work upgrade plan (T03–T32, strict order, D2) is the build order; a parallel-gowork task
that a T-task covers is **not built separately** — it is ticked when that T-task's proof lands,
citing the T-task. Only the four tasks marked "none" are built under D9.

| parallel-gowork | status after C1 | covered by | why |
|---|---|---|---|
| 1.1 task graph | done | T02, T03 | same task fields; T03 is the same cycle / unsafe-path / ownership-overlap validation (T03 must reuse `task_graph.py`, not rebuild it) |
| 1.2 ready scheduler | done | T06 | ready set across child plans; only accepted prerequisites release dependents |
| 1.3 run state | done | T05 | attempt identity, result commit, accepted-vs-finished |
| 2.1 worker workspace (branch + `.worktrees/wt-<thread>`) | open | **none** | the upgrade plan reuses the existing `work_copy.py` copies; no T-task owns this — D9 |
| 2.2 result verifier | open | T16 | real check/review evidence gates acceptance |
| 2.3 briefing | done | T12 | compact persisted worker handoff, no chat history |
| 3.1 coordinator: intent-first ready-set dispatch, no product cap | open | T08, T09, T10, T11 | cap removal + adaptive admission + dispatch through the coordinator |
| 3.2 integration owner | open | T15, T23 | serialized combination in review copies; automatic integration under a per-project lock |
| 3.3 core loop routes structured plans | open | T11 | same file/scope (`claude_code_core/task_loop.py`) |
| 3.4 Discord wiring (threads, queue/capacity notices, status, stop) | open | T13, T14, T22 | individual worker release, archive notices, outcome reporting |
| 3.5 thread lifecycle (archive after integration) | open | T14 | archive without deleting history, idempotent retry |
| 4.1 planning models | done | T01 | master/child identities, parent links |
| 4.2 planning registry | open | T04 | multiple builds without overwriting, atomic writes, legacy lookup |
| 4.3 split policy (clear / uncertain / keep-together) | open | **none** | no T-task decides splits — D9 |
| 4.4 child planning threads in Discord | open | **none** | T20/T21 are blocker questions, a different concern — D9 |
| 5.1 generic correlation/parent metadata + idempotent archive in `setup.py`/`api_server.py` | open | T14 (archive) | the correlation/parent fields are not owned by any T-task — the field part is D9 |
| 5.2 operator guidance (SKILL.md, coordinator.md) | open | **none** | T29 packages planner guidance for harnesses, not this extension's docs — D9 |
| 5.3 final gate | open | T32 | same final gate |

D9 therefore = parallel-gowork 2.1, 4.3, 4.4, 5.1 (metadata half), 5.2.
- T18: "every failure entry point shares the budget" names retries, smart_unstick, model escalation, splitting and goal-auto-round paths, which all belong to the legacy checkbox build → the manifest build has one automatic repair per task in the ledger (durable, one entry point); the checkbox build keeps its existing unstick ladder untouched → the two paths share no state (checkbox builds have no ledger), and rewriting the checkbox ladder onto the ledger is out of scope for a build that must keep old plan files working (Bootstrap Rules: compatibility includes old plan files and stored runs).

## D6: trusted-agent-handoffs

- 2.2: a handoff packet must fit one Discord message together with a short header (~1,100
  characters of goal + findings once the fixed fields are counted); a larger packet is refused
  at send time (`HandoffEnvelopeError`) rather than split across messages, because a starter
  that spans two messages cannot be found again by a reconnecting recipient. The protocol's
  6,000-byte bound stays as the transport-neutral limit.
- 3.1: `POST /api/project-lookup` used to let the runner's working directory beat
  `CCDB_PROJECT_LOOKUP_ROOT`; it now resolves through the same `lookup_root()` as the Discord
  path, so configuration wins and the working directory is only a fallback. The default lookup
  locator `drew/main-projects` is pinned to that root (not derived from an owner root), so the
  narrow slice keeps working with no new configuration; other locators need
  `CCDB_HANDOFF_PROJECT_ROOTS=owner=path,...`.
- 3.2: "materially unclear actions" is read as a keyword heuristic on the goal (open-ended
  phrases such as "whatever it takes", "full permissions", "etc."), erring toward blocking —
  a false positive costs one clarification in the job thread, a false negative would be an
  unapproved action. A packet that carries a capability the recipient policy forbids blocks
  visibly even when the goal never mentions it, instead of running a narrower task than asked.
- 3.3: the v1 packet has no session field, so "an explicitly named existing session" is an
  injectable `session_selector(task) -> thread id | None` on the executor; the shipped default
  returns `None`, i.e. every handoff runs as a fresh turn in its job thread. A restart resumes
  the interrupted attempt under the *same* attempt number (the claim row is closed as
  `interrupted` and reclaimed once) rather than spending a retry on the crash.
- 2.4: `on_ready` fires on every gateway reconnect, not only at startup, so the restart half of
  reconciliation (requeue `running` jobs) runs once per process while the starter scan runs on
  every reconnect — otherwise a job genuinely running in this process would be requeued
  underneath itself. Archived task threads are reached through the starter message (thread id
  == starter id) rather than a separate archived-thread walk.
