# v4.1.0: finish every open build (plan for David's computer)

Check: scripts/test-clean-env.sh tests/ -x -q
Try: uv run python -m claude_discord.main
Goal: Every planned-but-unfinished ccdb feature is built, tested and shipped together as v4.1.0 on branch `release/v4.1.0`, built entirely on David's computer.
Done when: every box below is ticked with proof in `docs/plans/v4.1.0-finish-all-builds.progress.md`, the full gate in HANDOFF-V4.1.0.md section 3 is green, version is 4.1.0, and `release/v4.1.0` is pushed with an open PR to `main`.

Read `HANDOFF-V4.1.0.md` first. Its rules (section 5) apply to every task. One box = one
outcome in one fresh session, about 15–30 minutes. A box that is really several outcomes
("build the remaining tasks of X") must be split into its own boxes **in this file**,
directly under it, before any code is written. Tick a box only after its proof is written
to the progress file. Work top to bottom; a later box may assume every earlier box is done.

## How to try it

1. `scripts/test-clean-env.sh` ends with `passed` and no `failed`.
2. `git log --oneline handoff/v4.1.0-base..release/v4.1.0` shows one commit per finished box.
3. In the DAVID'S COMPUTER Discord category (David's bot running this branch), `/help` lists the new commands and a normal message in a workers thread still gets an answer.

## A. Start

- [x] A1: Clone, create `release/v4.1.0` from `handoff/v4.1.0-base`, `uv sync --dev`, run the full gate (HANDOFF section 3) and record the baseline numbers in the progress file. They must match 4556 passed / 0 failed; if not, stop and fix the environment first.

## B. Bring in work that only exists on other branches

Commit lists for every branch: `docs/plans/v4.1.0-branch-inventory.md`.

- [x] B1: Merge `origin/save/local-main-2026-09-21` into `release/v4.1.0`. Two conflicts are known: `claude_discord/cogs/claude_chat.py` (keep both sides' behaviour) and `.planning/davids-computer/three-bot-flow.md` (keep the newer, fuller text). Proof: full test run green, plus `git grep` showing the new-project/GitHub clone menu, favorites launcher and control-center session controls are present.
- [x] B2: Bring in automatic voice transcripts from `origin/deploy/drew-ai-voice-transcripts` (commits `1baab0b`, `e581115`, `c5ac2cf`) only where the base lacks them (`voice_transcripts` does not exist in the base; the voice recorder does). Cherry-pick or port, with tests.
- [x] B3: Bring in `e5c3293 fix: accept structured worker test evidence` from `origin/lockin/feature-workflow-runtime` if the base's feature-workflow code lacks it; also review that branch's `docs/open-source-coding-agent-harness-alternatives.md` and keep it under `docs/research/`.
- [x] B4: Compare `extensions/project_picker/` and `examples/ebibot/cogs/workdir_command.py` (both came in with B1) against the base's project picker / workdir code. Keep one implementation; delete the duplicate; tests green.
- [x] B5: Walk every remaining branch in the inventory. For each commit, find the equivalent in `release/v4.1.0` (by feature, not by commit ID). Record "present / ported / dropped because …" per commit in the progress file. Port anything genuinely missing as its own box added below this one.

## C. Reconcile stale checkboxes before building

- [x] C1: For each of the seven OpenSpec changes (`discord-command-surface`, `trusted-agent-handoffs`, `shared-project-catalog`, `discord-my-ai-setup`, `professional-harness-audit`, `parallel-gowork`, `model-capacity-resilience`), compare `tasks.md` with the code and `.planning/2026-09-20-discord-agent-flow-program/wave-*-manifest.json`. Tick tasks whose code and tests already exist (cite file + test). Commit only the `tasks.md` updates.
- [x] C2: Decide the overlap between `openspec/changes/parallel-gowork/` and `docs/plans/gowork-upgrade-execution-plan.md` (both build parallel Go Work). Map each parallel-gowork task to the upgrade task(s) that cover it; mark covered ones "covered by Txx" so nothing is built twice. Write the mapping in `docs/plans/v4.1.0-decisions.md`.
- [x] C3: `trial-greeting`, `trial-word-count`, `folder-session-launcher`: verify what is implemented, tick it, and archive any change that is complete (`openspec archive`).
- [x] C4: `.planning/2026-09-10-land-everything/task_plan.md`: tick what is done, and for each remaining item either add a box to section D here or record why it is obsolete.

## D. Build

Each D box below is a parent: split it into one box per remaining task (from its own
`tasks.md` / plan) directly underneath, then build those one at a time. Tick the parent
only when all its children are ticked.

- [x] D1: Finish `folder-session-launcher` (3 open tasks), then archive it.
- [x] D2: Go Work upgrade — build T03 through T32 of `docs/plans/gowork-upgrade-execution-plan.md` **in its strict order**, following that file's own rules (it has its own Check line: `uv run python scripts/check_gowork_upgrade.py`). T03 was started: the WIP commit `b9773d3` holds unfinished plan-validation code and tests; finish or redo it. Tick the boxes in that plan file and add a proof entry to its `.progress.md` for each.
- [x] D3: `model-capacity-resilience` — remaining tasks. (Do this early: other features rely on not dead-ending at "model at capacity".)
- [x] D4: `discord-command-surface` — remaining tasks. (4.3/4.4 are live Discord acceptance → E5.)
- [x] D5: `shared-project-catalog` — remaining tasks. (5.2 live activation → E5.)
- [x] D6: `trusted-agent-handoffs` — remaining tasks (reuse `claude_discord/handoff_*.py`; project lookups already work end to end).
- [x] D7: `discord-my-ai-setup` — remaining tasks. (5.2 live activation → E5.)
- [x] D8: `professional-harness-audit` — remaining tasks.
- [x] D9: `parallel-gowork` — only the tasks C2 did not map to the Go Work upgrade.
- [x] D10: Any boxes added by B5 or C4.
  - [x] D10a: (from land-everything Task 8) Make the start-fresh nudge work on Codex: read `model_context_window` (and used tokens) from Codex's `token_count` event into the `turn.completed` `StreamEvent` in `claude_code_core/codex_runner.py`, so `event_processor.py` persists `context_window`/`context_used` for Codex threads. Failing test first in `tests/test_codex_runner.py`.
  - [x] D10b: (from land-everything Task 9) Make the start-fresh nudge work on DSH with an estimate: prompt + reply characters ÷ 4 accumulated per session against the model's window, labelled "estimate" wherever the percentage is shown. Failing test first.

## E. Prove it all works together

- [ ] E1: Full gate from HANDOFF section 3, all green. Record exact numbers.
- [ ] E2: Security audit per `.claude/skills/security-audit/SKILL.md` over the whole diff `handoff/v4.1.0-base..release/v4.1.0`; fix anything found.
- [ ] E3: Offline Go Work practice run: `uv run python scripts/check_gowork_upgrade.py` plus the practice command built in T31. Record output.
- [ ] E4: Update the program plan `.planning/2026-09-20-discord-agent-flow-program/task_plan.md` phases 3–4 and archive every completed OpenSpec change (`openspec archive <name>`), running `openspec validate --strict` first.
- [ ] E5: Live smoke test with **David's own bot** running this branch, in the DAVID'S COMPUTER category only: send a message, start a session from control-center, run one project lookup, and check `/help`. Never use Drew's DrewAI token. If David's bot can't be run, record that and skip.

## F. Release

- [ ] F1: Set `version = "4.1.0"` in `pyproject.toml` (and anywhere else the version is written — `git grep 4.0.26`). Move `CHANGELOG.md` `[Unreleased]` under `## [4.1.0] - <date>` and add one line per finished feature.
- [ ] F2: Write `docs/plans/v4.1.0-release-notes.md` in plain English: what's new, a 30-second Discord try-out for each feature, what was dropped and why, known issues. Also list the exact Lenovo switch-over commands from HANDOFF section 8.
- [ ] F3: Final full gate, then `git push -u origin release/v4.1.0` and `gh pr create --base main --title "Release v4.1.0" --body-file docs/plans/v4.1.0-release-notes.md`. Do not merge, do not tag. Post the PR link to Drew with one line saying it is ready to switch over.
