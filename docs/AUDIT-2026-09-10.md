# Lockin AI Discord bot audit, 2026-09-10

Read-only audit. It was run after the harness cleanup (`/home/drewp/main-projects/DESKTOP HOME/APPLY-EVERYTHING.md`) and doesn't redo it.

## Live breaks (fix first)

| # | What | Where |
|---|---|---|
| 1 | Hermes gateway still running from its archived binary; it has been writing to a re-created `~/.hermes/` | PID 537, `hermes-gateway-drewai.service` (cgroup still active) |
| 2 | Cron job for a Hermes script that no longer exists | `crontab -l` line 1 → `/home/drewp/.hermes/profiles/asset/cron/audio-cache-cleaner.sh` |
| 3 | Cron job at the wrong path; fails every 5 min | `/home/drewp/discord-control/idle-nudge.sh` (real path: `/home/drewp/main-projects/discord-control/`) |
| 4 | DSH turns in the bot never see `~/AGENTS.md` | `DSH_HOME` unset → `/home/drewp/.local/state/ccdb/dsh/AGENTS.md` missing (`claude_code_core/dsh_backend.py:147-156`) |
| 5 | Global skills differ by harness: Claude Code is missing `planning-with-files` | `/home/drewp/.claude/skills` vs `/home/drewp/.agents/skills` |

## Repos

- **`/home/drewp/main-projects/ebi-agent-chat-relay`**: the bot (ccdb). It runs Claude, Codex, local, DSH and AG-UI backends.
  - `/home/drewp/main-projects/Lockin AI` is a **symlink to it**, not a second repo.
  - Runs from `~/.config/systemd/user/ebi-agent-chat-relay.service`, plus the drop-in `concurrency.conf`, `scripts/pre-start.sh` and a 2-min health timer.
  - The default backend is `CCDB_BACKEND=codex`.
- **`/home/drewp/main-projects/discord-control`**: shell scripts for idle-nudge, create-channel and allowlist.
  - Only cron uses it, through the broken path in #3.
  - It has 5 uncommitted files; the `.nudge.log` diff alone is 1,562 lines.
- **`/home/drewp/main-projects/dsh-fleet`**: a DSH worker fleet plus a Telegram bridge, **built as a Hermes integration**.
  - All of its units are inactive and still point at `~/.hermes`.
  - It's dead without Hermes.
- **`/home/drewp/main-projects/dsh-mcp-router`**: a DSH MCP-router plugin. It isn't under git and nothing references it. Orphan.
- **`/home/drewp/main-projects/codex-machine-control-center`**: one commit, zero tracked files, 2 behind origin. Empty.
- **`/home/drewp/main-projects/ebi-agent-chat-relay-preserved-local`**: 3 stale doc snapshots from 2026-09-08. No code. Safe to archive.
- **Worktrees of the bot repo:**
  - `lockin-workflow-runtime`, `lockin-codex-native-harness-plan`, `lockin-concurrency-review`, `drew-ai-voice-runtime`, `.worktrees/wt-1547448027776811038`.
  - `concurrency.conf.before-canonical-main-20260908` still names `lockin-workflow-runtime` but isn't loaded.

## How each harness is invoked from Discord

| Harness | Spawn | Instructions | Global skills |
|---|---|---|---|
| Claude Code | `claude_code_core/runner.py:140` → `/home/drewp/.local/bin/claude` | `~/.claude/CLAUDE.md` = `@~/AGENTS.md` + cwd CLAUDE.md | `~/.claude/skills`: desktop-home, one-question |
| Codex | `claude_code_core/codex_runner.py:370` → `/home/drewp/.local/bin/codex`, `CODEX_HOME=/home/drewp/.codex-ccdb` | `~/.codex-ccdb/AGENTS.md` → `~/AGENTS.md` | `~/.agents/skills`: desktop-home, one-question, planning-with-files |
| DSH | in-process SDK, `claude_code_core/dsh_backend.py:481-520` (bundled runtime binary) | **none** (see #4) | `~/.agents/skills`: same 3 as Codex |

- The `CCDB_CLI_ENV_FILE` overlay is unset, so live config switching is inactive.
- Codex `config.toml` sets `danger-full-access` with approval `never`. That's intentional, but note it.

## Leftover Hermes/OpenCode references (all broken, none in the bot repo)

- **Inactive units:**
  - `~/.config/systemd/user/dsh-fleet-activate.service:3,7`
  - `~/.config/systemd/user/dsh-telegram-bridge.service:3,8`, plus its drop-in `followthrough.conf:3`
  - `~/.config/systemd/user/jina-proxy.service:8-9`
- **Shell:** `~/.bashrc:146-147` adds the missing `$HOME/.opencode/bin` to PATH.
- **dsh-fleet code:**
  - `bridge/bridge.py:95`
  - `scripts/closeout_precheck.py:244`
  - `scripts/activate_when_idle.py:43,68,84,105`
  - `integrations/hermes/*`
- **Clean:** the bot repo, its `.env`, `~/.codex-ccdb/config.toml`, `~/.claude/settings.json` and `~/AGENTS.md`.

## Dead, duplicated or fragile code in the bot repo

- **Duplicate cogs:** `extensions/project_picker/` (untracked) duplicates `examples/ebibot/cogs/workdir_command.py` (untracked) and `voice_recorder.py` byte-for-byte. `CUSTOM_COGS_DIR` loads only `examples/ebibot/cogs`, so the extensions copy is dead and its tests sit outside `tests/`.
- **Skill copy:** `extensions/feature_workflow/skill/` is a tracked copy of `lockin-feature-workflow`.
- **Skill symlinks outside the repo:** the untracked `.agents/skills/{lockin-feature-workflow,parallel-feature-development,task-coordination-strategies}` are absolute symlinks into `/home/drewp/AISkills/legacy/`.
- **Same name, different files:** `examples/ebibot/cogs/auto_upgrade.py` (40 lines) and `claude_discord/cogs/auto_upgrade.py` (606 lines).
- **Uncommitted work at risk:** `claude_code_core/dsh_backend.py` has +49 lines (`_bind_coordination_values`, `_runtime_env` injecting `CCDB_API_SECRET`), with its tests uncommitted too. `pre-start.sh` pulls on restart, so this work is at risk.
- **Hardcoded path:** `examples/ebibot/cogs/voice_recorder.py:34` hardcodes `/home/drewp/main-projects/audio-content`.
- **Conflicting config:** `.env` `MAX_CONCURRENT_SESSIONS=3` is silently overridden to 10 by the systemd drop-in.
