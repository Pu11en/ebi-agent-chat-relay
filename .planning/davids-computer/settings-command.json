{
  "command": "/settings",
  "status": "design_in_progress_not_implemented",
  "required_arguments": [],
  "navigation": {
    "clickable_options": true,
    "typed_search": true,
    "implementation_note": "Choose a Discord-supported search/picker interaction during implementation; do not promise live filtering without verifying it."
  },
  "control_center": {
    "scope": "current_category_defaults_for_future_sessions",
    "existing_sessions": "retain current settings",
    "default_agent_and_model": "available here",
    "choose_existing_thread": "not part of current agreed control-center settings flow after user correction"
  },
  "session_thread": {
    "status": "separate options not yet defined",
    "model_changes": "excluded; use /switch only",
    "does_not_change": "category defaults or other sessions"
  },
  "switch_shortcut": {
    "command": "/switch",
    "purpose": "quick agent/model selection",
    "default_location": "current session thread",
    "settings_overlap": "No model selector in thread settings; control-center settings handles future-session defaults only.",
    "selection_flow": "Choose a model; resolve its harness automatically, with no separate harness step."
  },
  "next_question": null,
  "user_correction": "Thread settings are not for model changes; thread session commands differ from control-center commands.",
  "default_model_picker": {
    "initial_view": "favorites_and_recent",
    "typed_search": true,
    "click_selection": true,
    "user_answer": "A: favorites and recent",
    "scope": "future sessions in current category only",
    "existing_sessions": "unchanged",
    "favorite_ownership": "not_yet_decided",
    "selection_unit": "model",
    "group_by_harness": false,
    "separate_harness_selection": false,
    "harness_resolution": "Automatically select the compatible configured harness associated with the chosen model on the current category computer."
  },
  "user_corrections": [
    "Do not organize model selection by harness; select the harness automatically from the chosen model."
  ],
  "deferred_decisions": [
    "Whether model favorites and recents are personal or shared; latest correction was about harness selection, not ownership."
  ]
}
