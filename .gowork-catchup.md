# Gowork v3 — where things stand

## ✅ What's live right now
- **Running in the bot since the 3:51 pm restart today**, from the test copy (not on GitHub yet)
- **Each build talks in its own thread**; the starting channel only gets the start card and the final result
- **Only a bare "close" or "stop" ends a build**; "pause" is passed to the AI
- **Goals:** a plan's goal reaches every step; plans with no goal start with a short interview (up to 5 lettered questions)
- **If the goal isn't met**, the build adds its own missing steps, up to 3 rounds, then asks you
- **Option "A" lets the AI pick the model for each step**, staying inside Claude only
- **Up to 3 independent steps run side by side**
- **Stuck steps** try the strongest model, then split into smaller steps, then ask you
- **Only hard steps get reviewed**; "careful" mode reviews every step
- **Three modes:** cheap, balanced (the default) and careful
- **"Queue it"** runs builds one after another, with an 8 am summary
- **It learns:** it tracks how each AI did, and the finished card gives "Next time" tips
- **After the last step** the thread becomes a normal chat; "looks good" keeps everything
- **All 3,392 tests pass**

## ✅ Proven in real practice runs
- The build thread, step messages, the work check, side-by-side steps, "next time" notes, the finished card, the chat afterward, and keeping the work
- **Fixed from those runs:** the helper was calling the wrong AI program, questions with choices underneath weren't noticed, and waiting for your answer gave up after 10 minutes (it now waits 12 hours)

## ⬜ Built but never tried for real
- The goal interview going all the way to a saved goal
- The AI picking the model per step (option "A")
- A reviewer sending a step back
- A stuck step getting unstuck
- The queue and its 8 am summary
- Cheap and careful modes

## ⚠️ Open items
- **The small helper AI only works with Claude.** If you ever stop using Claude, the model picker, step grouping, hard-step check and tips quietly switch off. Fix: use the cheapest model of whatever AI the build uses
- **Allwork's night runs are off** (you had those 27 threads deleted). They come back only if you ask, and into the single Allwork thread
- **Shipcheck's finished work (S1–S7) is still on its own unmerged branch**; you're starting a separate thread to make shipcheck public
- **Small gaps:** typing while the goal interview is still writing counts as a note to the build; a restart during side-by-side steps can leave empty threads behind; "next time" tips only cover steps since the last restart
- **Putting it on GitHub** waits for your OK after you've tried it
